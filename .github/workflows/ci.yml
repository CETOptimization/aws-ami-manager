name: CI

on:
  workflow_dispatch:
  push:
    branches: ["**"]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26.x'
          cache: true

      - name: Setup just
        uses: extractions/setup-just@v3

      - name: Tidy
        run: just tidy

      - name: Vet
        run: just vet

      - name: Test
        run: just test

      - name: Build
        run: just build

